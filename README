This utility creates a tar.gz for arbitrary files in a single directory, copies files to a local or remote share
then deletes files in the destination if older than 7 days based on the timestamp in the filename.  I did not use system
timestamps intentionally, call me crazy.
